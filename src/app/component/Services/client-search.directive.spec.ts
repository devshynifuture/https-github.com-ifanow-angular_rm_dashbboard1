import { ClientSearchDirective } from './client-search.directive';

describe('ClientSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new ClientSearchDirective();
    expect(directive).toBeTruthy();
  });
});
