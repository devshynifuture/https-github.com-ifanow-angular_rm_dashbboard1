import { HolderNameDirective } from './holder-name.directive';

describe('HolderNameDirective', () => {
  it('should create an instance', () => {
    const directive = new HolderNameDirective();
    expect(directive).toBeTruthy();
  });
});
