import { AlphanumricDirective } from './alphanumric.directive';

describe('AlphanumricDirective', () => {
  it('should create an instance', () => {
    const directive = new AlphanumricDirective();
    expect(directive).toBeTruthy();
  });
});
