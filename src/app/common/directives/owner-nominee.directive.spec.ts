import { OwnerNomineeDirective } from './owner-nominee.directive';

describe('OwnerNomineeDirective', () => {
  it('should create an instance', () => {
    const directive = new OwnerNomineeDirective();
    expect(directive).toBeTruthy();
  });
});
